#include <cstdio>
#include <iostream>
using namespace std;
const int maxn = 505;
int a[maxn];
int dp[2][maxn][maxn];
int main() {
  int n;
  scanf("%d", &n);
  bool flg = 0;
  for (int i = 0; i < n; i++) {
    scanf("%d", &a[i]);
    for (int j = 0; j <= i; j++) {
      for (int k=0)
    }
  }
  return 0;
}