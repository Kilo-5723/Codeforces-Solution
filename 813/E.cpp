#include <cstdio>
#include <iostream>
#include <optional>
#include <queue>
using namespace std;
const int maxn = 1e5 + 5;
struct node {};
int main() {
  int n, k;
  scanf("%d%d", &n, &k);
  vector<queue<int>> que(maxn);
  for (int i=0;i<n;i++){
    
  }
  return 0;
}